# ==============================================================================
# Local Docker Compose Environment
# ==============================================================================
#
# Environment variables for use with OpenCloudHub/local-compose-stack.
# This is the "quick & dirty" option for rapid local development.
#
# Prerequisites:
#   1. Clone and start the local stack:
#      git clone https://github.com/OpenCloudHub/local-compose-stack.git
#      cd local-compose-stack && docker compose up -d
#
#   2. Load these environment variables:
#      set -a && source .env.docker && set +a
#
#   3. Start Ray head node:
#      ray start --head
#
# Services:
#   - MLflow UI:      http://localhost:5000
#   - MinIO Console:  http://localhost:9001 (admin/admin123)
#   - MinIO API:      http://localhost:9000
#
# ==============================================================================

# MinIO / S3 / DVC
AWS_ACCESS_KEY_ID=admin
AWS_SECRET_ACCESS_KEY=admin123
AWS_ENDPOINT_URL=http://localhost:9000
DVC_REPO=https://github.com/OpenCloudHub/data-registry
DVC_REMOTE_URL=s3://dvcstore
DVC_DATA_VERSION=wine-quality-v1.0.0

# MLflow
MLFLOW_TRACKING_URI=http://localhost:8081
MLFLOW_EXPERIMENT_NAME=wine-quality
MLFLOW_REGISTERED_MODEL_NAME=ci.wine-classifier
MLFLOW_ENABLE_SYSTEM_METRICS_LOGGING=true

# Tracking
DOCKER_IMAGE_TAG=DEV
ARGO_WORKFLOW_UID=DEV
